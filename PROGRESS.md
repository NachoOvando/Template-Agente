# Progreso — Template de agente conversacional

## Estado actual

Template genericizado, recién forkeado desde la instancia original ("Cata",
Visit Catamarca). El historial completo de esa instancia — decisiones,
prompts reales, deploy a producción, bugs encontrados y corregidos — vive en
`git log` antes de la genericización. Este archivo arranca vacío para el
próximo fork/proyecto que use este template.

## Producción

Sin desplegar todavía en esta instancia del template. Ver "Cómo forkear este
template" en `CLAUDE.md` para el checklist antes de desplegar.

## Historial

(vacío — el historial de la instancia original está en `git log`)

## Falta

(definir según el dominio del fork)

## Bloqueadores

Ninguno.
